import { NotFoundView } from "@/components/not-found-view";

/** Dentro do portal: mantém cabeçalho e menu (pedido de outro cliente, link antigo etc.). */
export default function NotFound() {
  return <NotFoundView />;
}
